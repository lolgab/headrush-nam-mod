/* miniz_export.h -- normally CMake-generated by miniz's own build (visibility
 * control for building it as a shared library). We always statically link
 * it as part of nam_gui_core, so no export/import decoration is needed. */
#ifndef MINIZ_EXPORT_H
#define MINIZ_EXPORT_H

#define MINIZ_EXPORT

#endif
